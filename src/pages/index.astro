---
import { Image } from 'astro:assets';
import SiteLayout from '../layouts/SiteLayout.astro';
import importShot from '../assets/screenshots/import.png';
import rulesShot from '../assets/screenshots/rules.png';
import dashboardShot from '../assets/screenshots/dashboard.png';

const steps = [
	{
		title: 'Import',
		text: 'Upload an Excel, CSV, or PDF bank statement. Every row goes through a preview where you can review, edit, or exclude it before anything is added to your account.',
	},
	{
		title: 'Automate',
		text: 'Set a rule once and it runs on every future import — match transaction text and assign a category, type, or tags automatically.',
	},
	{
		title: 'Understand',
		text: 'A dashboard shows income, expenses, and net savings, with category breakdowns and month-over-month trends.',
	},
];

const features = [
	{
		title: 'Bank statement import',
		text: 'Column mapping is remembered per bank, duplicate transactions are caught automatically, and nothing is added to your account without a review step first.',
		formats: ['.xlsx', '.xls', '.csv', '.pdf'],
	},
	{
		title: 'Automation rules',
		text: 'Rules match transaction text — contains, equals, starts with, or ends with — and run in priority order, assigning a category and optionally a type or tags. Recurring transactions sort themselves.',
	},
	{
		title: 'Dashboard & insights',
		text: 'Income, expenses, net savings, and transaction count at a glance, plus category breakdowns, daily trends, and monthly comparisons.',
	},
];

const previews = [
	{
		title: 'Bank statement import',
		caption: 'Upload a file, map its columns once, and Vezne remembers the mapping for next time.',
		image: importShot,
		alt: 'Vezne import screen with a file upload field and saved column mapping selector',
	},
	{
		title: 'Automation rules',
		caption: 'Priority-ordered rules match transaction text and assign a category automatically.',
		image: rulesShot,
		alt: 'Vezne rules table listing three automation rules with their match text and assigned category',
	},
	{
		title: 'Dashboard & insights',
		caption: 'Income, expenses, and net savings over time, plus a category breakdown.',
		image: dashboardShot,
		alt: 'Vezne dashboard with income, expense, and net savings charts and a category breakdown',
	},
];
---

<SiteLayout
	title="Personal finance tracker"
	description="Vezne is a personal finance tracker — import bank statements, automate categorization with rules, and see where your money goes."
	wide
>
	<section class="hero">
		<h1>Track your money without connecting your bank.</h1>
		<p class="lede">
			Import your bank statement files, set rules that categorize new
			transactions automatically, and see exactly where your money goes.
		</p>
		<div class="hero-actions">
			<a class="cta-primary" href="https://app.vezne.app/register">Go to app →</a>
			<a class="cta-secondary" href="/faq">See how it works</a>
		</div>
		<p class="trust">
			No bank connection, ever — Vezne only sees the statement files you
			choose to upload.
		</p>
	</section>

	<section class="steps">
		{
			steps.map((step, i) => (
				<div class="step">
					<span class="step-index">{String(i + 1).padStart(2, '0')}</span>
					<h3>{step.title}</h3>
					<p>{step.text}</p>
				</div>
			))
		}
	</section>

	<section class="features">
		{
			features.map((feature) => (
				<div class="feature">
					<h3>{feature.title}</h3>
					<p>{feature.text}</p>
					{feature.formats && (
						<ul class="formats">
							{feature.formats.map((format) => (
								<li>{format}</li>
							))}
						</ul>
					)}
				</div>
			))
		}
	</section>

	<section class="previews">
		<h2 class="previews-heading">See it in action</h2>
		{
			previews.map((preview) => (
				<div class="preview">
					<p class="preview-title">{preview.title}</p>
					<div class="preview-frame">
						<Image src={preview.image} alt={preview.alt} />
					</div>
					<p class="preview-caption">{preview.caption}</p>
				</div>
			))
		}
	</section>
</SiteLayout>

<style>
	.hero {
		max-width: 640px;
		margin: 2rem 0 4rem;
	}

	.hero h1 {
		margin: 0 0 1rem;
		font-size: 2.75rem;
		font-weight: 600;
		line-height: 1.15;
		letter-spacing: -0.02em;
	}

	.lede {
		margin: 0 0 1.75rem;
		font-size: 1.1rem;
		line-height: 1.6;
		color: #a39e95;
	}

	.hero-actions {
		display: flex;
		align-items: center;
		gap: 1.5rem;
		margin-bottom: 1.25rem;
	}

	.cta-primary {
		color: #1a0d04;
		background: #c17b45;
		text-decoration: none;
		font-size: 0.95rem;
		font-weight: 600;
		padding: 0.7rem 1.25rem;
		border-radius: 8px;
	}

	.cta-primary:hover {
		background: #d99a6c;
	}

	.cta-secondary {
		color: #ece7df;
		text-decoration: none;
		font-size: 0.9rem;
		border-bottom: 1px solid rgba(236, 231, 223, 0.3);
		padding-bottom: 2px;
	}

	.cta-secondary:hover {
		border-bottom-color: #ece7df;
	}

	.trust {
		margin: 0;
		font-size: 0.85rem;
		color: #a39e95;
	}

	.steps {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2rem;
		padding: 2.5rem 0;
		border-top: 1px solid rgba(236, 231, 223, 0.08);
		border-bottom: 1px solid rgba(236, 231, 223, 0.08);
		margin-bottom: 4rem;
	}

	.step-index {
		display: block;
		font-size: 0.8rem;
		font-weight: 600;
		letter-spacing: 0.08em;
		color: #c17b45;
		margin-bottom: 0.75rem;
	}

	.step h3 {
		margin: 0 0 0.5rem;
		font-size: 1.05rem;
		font-weight: 600;
	}

	.step p {
		margin: 0;
		font-size: 0.9rem;
		line-height: 1.6;
		color: #a39e95;
	}

	.features {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1.5rem;
		margin-bottom: 3rem;
	}

	.feature {
		background: #100e0b;
		border: 1px solid rgba(236, 231, 223, 0.08);
		border-radius: 12px;
		padding: 1.5rem;
	}

	.feature h3 {
		margin: 0 0 0.6rem;
		font-size: 1rem;
		font-weight: 600;
	}

	.feature p {
		margin: 0;
		font-size: 0.9rem;
		line-height: 1.6;
		color: #a39e95;
	}

	.formats {
		list-style: none;
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
		margin: 1rem 0 0;
		padding: 0;
	}

	.formats li {
		font-size: 0.75rem;
		color: #a39e95;
		border: 1px solid rgba(236, 231, 223, 0.12);
		border-radius: 6px;
		padding: 0.15rem 0.5rem;
	}

	.previews {
		margin: 0 0 3rem;
	}

	.previews-heading {
		margin: 0 0 2rem;
		font-size: 0.8rem;
		font-weight: 600;
		letter-spacing: 0.08em;
		text-transform: uppercase;
		color: #a39e95;
	}

	.preview-frame {
		max-width: 900px;
	}

	.preview + .preview {
		margin-top: 3rem;
	}

	.preview-title {
		margin: 0 0 0.75rem;
		font-size: 1rem;
		font-weight: 600;
	}

	.preview-frame {
		background: #100e0b;
		border: 1px solid rgba(236, 231, 223, 0.08);
		border-radius: 12px;
		overflow: hidden;
		line-height: 0;
	}

	.preview-frame :global(img) {
		display: block;
		width: 100%;
		height: auto;
	}

	.preview-caption {
		margin: 0.75rem 0 0;
		font-size: 0.85rem;
		line-height: 1.6;
		color: #a39e95;
	}

	@media (max-width: 760px) {
		.hero h1 {
			font-size: 2rem;
		}

		.steps,
		.features {
			grid-template-columns: 1fr;
		}
	}
</style>
